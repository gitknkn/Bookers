<h２>Editing Book</h２>

<%= form_for(@book) do |f| %>
  <h4>Title</h4>
  <p><%= f.text_field :title %></p>

  <h4>Body</h4>
  <p><%= f.text_area :body %></p>

  <%= f.submit 'update Book' %>
<% end %>

  <span>
    <%= link_to 'show', book_path(@book.id) %>
  </span>
  <span>
    <%= link_to 'back', :back %> 
  </span>